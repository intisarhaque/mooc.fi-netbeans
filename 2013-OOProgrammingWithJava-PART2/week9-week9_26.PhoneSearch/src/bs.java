/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author intis
 */
/*
int x=0;
        if (!numbers.containsKey(key)&&!address.containsKey(key))
        {
            x+=1;
            for (String name: address.keySet())
            {
                if (!address.get(name).contains(key))
                {
                    x+=1;
                   System.out.println(" keyword not found");
                }
            }
            
        }
        System.out.println("x");
        if (x<2)
        {
            for (String name: numbers.keySet())
        {
            if (name.contains(key))
            {
                names.add(name);
            }
            
        }
        for (String name: address.keySet())
        {
            if (address.get(name).contains(key))
            {
                if (!names.contains(name))
                {
                    names.add(name);
                }
            }
            
        }
        }
        Collections.sort(names);
        for (String name:names)
        {
            System.out.println(" " + name);
            ArrayList<String> personNum = new ArrayList<String>();
            if (address.containsKey(name))
            {   
                System.out.println("  address: " + address.get(name));
            }
            else
            {
                System.out.println("  address unknown");
            }
            if (numbers.containsKey(name))
            {

                for (String number: numbers.get(name))
                {
                    personNum.add(number);
                }
                System.out.println("  phone numbers:");
                for (String numb: numbers.get(name))
                {
                    System.out.println("   " + numb );
                }
            }
            else
            {
                System.out.println("  phone number not found");
            }
            System.out.println("");     

        }
        
      
*/        